#!/usr/bin/expect -f
set timeout 120
set password "Sipanda123#"
spawn ssh -o StrictHostKeyChecking=no sipanda@157.10.252.74
expect "password:"
send "$password\r"
expect "$ "
send "grep -r 'admin' /etc/nginx\r"
expect "$ "
send "ls /home\r"
expect "$ "
send "netstat -tulpn | grep nginx\r"
expect "$ "
send "ls -la /var/www/html\r"
expect "$ "
send "exit\r"
expect eof
